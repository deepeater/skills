---
name: deepeater-wiki
description: "食悟 AI 项目知识库与方向对齐 skill。用于项目文档方向校准、员工了解项目基本情况、在飞书知识库中只读检索 AI 开发部、市场营销部、餐饮业务部、公司信息等资料，并按资源类型路由到对应 Lark skill。"
metadata:
  requires:
    skills: ["lark-shared", "lark-wiki", "lark-doc", "lark-drive", "lark-base"]
    optionalSkills: ["lark-sheets"]
    bins: ["lark-cli"]
  readonly: true
---

# deepeater-wiki

食悟 AI 项目的独立知识库与方向对齐 skill。它自带最小项目事实基线，不依赖外部共享基线 skill。

## 使用边界

- 本 skill 默认只读：只检索、引用、总结知识库内容。
- 禁止通过本 skill 新增、覆盖、删除或移动飞书知识库内容。
- 用户要求写入、整理、迁移、删除知识库时，说明 `deepeater-wiki` 不执行写操作；如确需继续，必须显式切换到对应 Lark 写操作并按高风险流程确认。
- 回答项目相关问题时，先用下方项目基线校准方向，再按需检索飞书知识库。

## 项目基线

| 项目 | 内容 |
|------|------|
| 产品名 | 食悟 AI |
| 定位 | 面向餐饮行业的智能化产品矩阵 |
| 核心客群 | 中小餐饮门店、成熟运营单店、增长型营销门店、区域连锁品牌 |
| 核心抓手 | 语音访台、超级私域 |
| 重点模块 | 智能巡店、AI 店长日报、营销日历、餐台预定、盈利算盘、AI 菜品研发、美食摄影、智能人事、知识库 |
| 架构边界 | 业务功能围绕餐饮场景，不扩展成跨行业通用 AI 平台 |

## 表达口径

- 语气：务实、落地、不浮夸。
- 重点：说明如何解决门店经营问题，而不是展示 AI 概念。
- 对外材料优先围绕餐饮行业、语音访台、超级私域、门店经营效率、复购增长、标准化管理展开。
- 避免空泛词：赋能、颠覆、一站式、AI 驱动。
- 计划书中的融资预测、十年财务目标、门店数量目标等动态指标不要直接当成长期事实；用户明确要融资材料时，再读取最新原始资料或飞书文档。

## 飞书知识入口

| 入口 | node_token | space_id | 当前节点 | obj_type | 用途 |
|------|------------|----------|----------|----------|------|
| AI 开发部 | `PKHwwIFwGiusIPktN7qcsAHZneb` | `7575877509762288595` | 2026年食悟AI公司规划表 | `bitable` | 研发规划、项目计划、开发资料 |
| 市场营销部 | `R0sxwewSJiUXfBklT5hcDUo9n7e` | `7528390383661367298` | 部门职责 | `docx` | 品牌、渠道、活动、内容、新人任务 |
| 餐饮业务部 | `XFfiwavjxiQc5zkgg7ycpHgsn6b` | `7523570528028884996` | 部门职责 | `docx` | 餐饮业务、门店场景、行业资料 |
| 公司信息 | `Rn3jwtDihiYrJyk7dCVcBpSBnNf` | `7523514647175888899` | 公司介绍 | `docx` | 公司介绍、组织信息、员工通讯录 |

## 只读检索流程

1. 根据用户问题选择入口：
   - 技术、研发、开发计划、AI 能力：AI 开发部。
   - 品牌、文案、活动、渠道、新人任务：市场营销部。
   - 门店、餐饮场景、业务 SOP、行业知识：餐饮业务部。
   - 公司介绍、组织、员工基础信息：公司信息。
2. 解析节点，确认当前资源类型：
   ```bash
   lark-cli wiki spaces get_node --params '{"token":"<node_token>"}' --format json --as user
   ```
3. 节点有子节点时，下钻目录：
   ```bash
   lark-cli wiki nodes list --params '{"space_id":"<space_id>","parent_node_token":"<node_token>"}' --page-all --format json --as user
   ```
4. 按 `obj_type` 路由：
   - `docx` / `doc`：使用 `lark-doc`，读取时必须带 `--api-version v2`，优先 `outline`，再用 `keyword` 或 `section` 精读，最后才读全文。
   - `bitable`：使用 `lark-base` 读取多维表格结构和记录，不要用 `docs +fetch`。
   - `sheet`：使用 `lark-sheets`。
   - `file`：使用 `lark-drive` 查看元数据或下载后处理。
5. 输出结论时必须标注来源，至少包含入口、节点标题、资源类型。资料不足时说明“基于当前可读知识库结果”。

## 常见任务处理

- 写项目介绍：先使用项目基线和表达口径，再检索“公司信息”补充最新公司资料。
- 写产品或业务文档：先对齐餐饮行业、语音访台、超级私域、门店经营问题，再按需检索 AI 开发部或餐饮业务部。
- 查市场资料：优先检索市场营销部，必要时补充公司信息。
- 查新人任务或部门任务：优先列出对应部门子节点，遇到 `bitable` 切到 `lark-base`。
- 用户要求投喂新知识：说明本 skill 只读，不直接写入知识库。
